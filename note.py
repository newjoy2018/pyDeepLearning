#---------------------------------------------------------------------------------
# 在Jupyter中解压.zip文件
#---------------------------------------------------------------------------------
import zipfile
f = zipfile.ZipFile("./test.zip",'r') # 原压缩文件在服务器的位置
for file in f.namelist():
    f.extract(file,"./") #解压到的位置，./表示当前目录(与此.ipynb文件同一个目录)

#---------------------------------------------------------------------------------
